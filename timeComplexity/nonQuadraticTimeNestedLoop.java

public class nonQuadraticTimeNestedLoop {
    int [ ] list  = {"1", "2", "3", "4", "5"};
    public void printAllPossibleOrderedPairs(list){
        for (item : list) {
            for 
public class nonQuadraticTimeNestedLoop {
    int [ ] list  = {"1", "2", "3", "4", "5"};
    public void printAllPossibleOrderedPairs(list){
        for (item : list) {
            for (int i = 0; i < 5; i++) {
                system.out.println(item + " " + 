package timeComplexity;

public class nonQuadraticTimeNestedLoop {
    int [ ] list  = {"1", "2", "3", "4", "5"};
    public void printAllPossibleOrderedPairs(list){
        for (item : list) {
            for (int i = 0; i < 5; i++) {
                system.out.println(item + " " + i)
    };
        };
}
